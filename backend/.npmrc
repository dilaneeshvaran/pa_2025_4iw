# @ts-rest/fastify declares a peer of fastify@^4 but works fine with fastify@5.
# The CI isolates this workspace (removes root manifests) and runs `npm ci` here,
# so the flag must live in backend/, not only at the repo root.
legacy-peer-deps=true
